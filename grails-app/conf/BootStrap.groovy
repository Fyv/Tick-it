import com.tickit.Role
import com.tickit.Utilisateur
import com.tickit.UserRole

class BootStrap {

   def init = { servletContext ->

//      def adminRole = new Role(authority: 'ROLE_ADMIN').save(flush: true)
//      def userRole = new Role(authority: 'ROLE_USER').save(flush: true)
//
//      def testUser = new Utilisateur(username: 'me', enabled: true, password: 'password')
//      testUser.save(flush: true)
//
//	  def admin = new Utilisateur(username: 'admin',  enabled: true, password: 'password')
//	  admin.save()
//	  
//      UserRole.create( testUser, userRole, true )
//      UserRole.create( admin, adminRole, true )
   }
}